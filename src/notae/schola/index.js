/**
 * Compute analysis metrics (ambitus, cadence points, ictus density).
 * @param {any} parsed
 * @returns {{ ambitus?:number[], cadence?:any[], ictusDensity?:number }}
 */
export function schola(_parsed) {
  // TODO (Step 9)
  return { ictusDensity: 0 };
}
