/**
 * Align and render words/text.
 * @param {any} parsed
 * @param {{}} [options]
 * @returns {string|string[]}
 */
export function verbum(_parsed, _options = {}) {
  // TODO (Step 10)
  return "";
}
