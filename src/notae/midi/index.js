/**
 * Convert parsed notes to MIDI events.
 * @param {any} parsed
 * @param {{ tempo?: number }} [options]
 * @returns {Array<{t:number, on?:number, off?:number}>}
 */
export function midi(_parsed, _options = {}) {
  // TODO (Step 8)
  return [];
}
