/**
 * Build a tuning map.
 * @param {{ mode?: string, scale?: string, comma?: string, LA4?: number }} [options]
 * @returns {Record<string, number>} pitchName->Hz
 */
export function temper(_options = {}) {
  // TODO (Step 6)
  return {};
}
